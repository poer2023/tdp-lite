# Publisher service listens locally on 3100 by default
NEXT_PUBLIC_PUBLISHER_URL=http://localhost:3100

# Target site (tdp-lite) base URL
PUBLISH_TARGET_BASE_URL=http://localhost:3000

# Internal Go API key from tdp-lite (scopes: media:write, preview:write, content:write)
TDP_INTERNAL_KEY_ID=
TDP_INTERNAL_KEY_SECRET=

# Legacy aliases (optional fallback for old env names)
PUBLISHER_KEY_ID=
PUBLISHER_KEY_SECRET=
