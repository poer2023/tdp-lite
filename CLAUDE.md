# TDP Lite

极简个人博客平台，采用"纸质极简"设计语言。

## 技术栈

- Next.js 16 + TypeScript
- Tailwind CSS
- Space Grotesk / Crimson Pro / JetBrains Mono 字体

## 项目结构

```
src/
├── app/[locale]/          # 国际化页面
├── components/bento/      # BentoGrid 卡片系统
│   ├── BentoGrid.tsx      # 主容器
│   ├── types.ts           # 类型定义
│   └── cards/             # 各类卡片组件
├── lib/
│   ├── schema.ts          # 数据类型
│   └── utils.ts           # 工具函数
└── app/globals.css        # 全局样式
```

## 设计语言

- **纸质纹理**: paper-card, paper-texture, paper-tint-1/2/3
- **墨水色系**: ink (#111), ink-light (#666)
- **阴影系统**: paper-sm, paper, paper-hover, paper-stack
- **字体分工**: font-display(标题) / font-serif(装饰) / font-mono(元信息)

---

# 待改进项 (Agent Team Review 2026-02-06)

## 🔴 高优先级

### 1. 图片组件未优化
**问题**: 使用原生 `<img>` 而非 `next/image`，影响 LCP 性能
**文件**: PostCard.tsx, MomentCard.tsx, GalleryCard.tsx
**修复**: 替换为 Next.js Image 组件，添加 width/height 或 fill 属性

### 2. CSS 变量重复定义
**问题**: `globals.css` 中有两处 `:root` 定义，`--paper-white` 值不一致
**位置**: 第 9-16 行 和 第 80-85 行
**修复**: 合并为一处，统一 `--paper-white: #fdfdfd`

### 3. 冗余 size-* 类
**问题**: `globals.css:72-78` 手写了 `size-*` 类，但 Tailwind v3.4+ 已内置
**修复**: 删除手写的 size-* 类

## 🟡 中优先级

### 4. 抽取公共组件
**问题**: Badge/Tag 样式在多个卡片中重复
```tsx
// 重复出现的样式
"inline-flex items-center gap-1.5 rounded-full bg-white/20 px-3 py-1 text-xs font-medium text-white backdrop-blur-md"
```
**修复**: 创建 `Badge` 组件

### 5. 卡片容器样式重复
**问题**: 多个卡片都有类似的容器样式
**修复**: 创建 `BaseCard` 组件或统一 CSS 类

### 6. Tailwind 配置不完整
- Crimson Pro 未注册到 `fontFamily.serif`
- `pulse-dot` 动画未注册到 Tailwind animation
**文件**: tailwind.config.ts

### 7. 阴影使用不统一
**问题**: 部分阴影直接写在行内样式（如 page.tsx:113）而非使用 Tailwind 预设
**修复**: 统一使用 shadow-paper-* 系列

## 🟢 低优先级

### 8. 深色模式不完整
**问题**: MomentCard 部分变体有 `dark:` 样式，其他卡片未适配
**修复**: 后续迭代统一添加深色模式支持

### 9. 硬编码值
- `PostCard:106` - "Read time: 5m" 应从 props 获取
- `GalleryCard:49` - "ISO 400 • f/1.8" fallback
- `MomentDetailCard:66` - "35MM • F/2.8" 硬编码

### 10. ActionItem 类型位置
**问题**: `ActionItem` 定义在 types.ts 而非 schema.ts，与其他类型来源不一致

### 11. MomentCard variant 类型
**问题**: `variant` 类型硬编码在组件内部，应提升到 types.ts

---

# 设计评分 (2026-02-06)

| 维度 | 评分 | 备注 |
|------|------|------|
| 纸质纹理 | 9/10 | 多层次效果精致 |
| 字体排版 | 9/10 | 三种字体分工明确 |
| 动画效果 | 9/10 | 流畅自然 |
| 组件结构 | 8/10 | 职责分离清晰 |
| 颜色系统 | 8/10 | 变量化程度高 |
| 类型完善度 | 7/10 | 部分类型位置不当 |
| Tailwind 配置 | 6/10 | 存在重复和遗漏 |
| 性能优化 | 4/10 | 图片未优化 |

## 亮点

- GalleryCard 顶部"胶带"效果创意十足
- 音乐卡片多频率 pulse 动画设计精巧
- 三级纸张色调（tint-1/2/3）层次分明
- bento-card hover 动画使用 cubic-bezier 缓动
